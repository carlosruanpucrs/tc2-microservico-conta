spring:
  application:
    name: conta-service
  data:
    mongodb:
      uri: mongodb+srv://devcarlosruan:lckdLSke2RdTSN9v@microsservico.jteqv7e.mongodb.net/microsservico?authSource=admin

server:
  port: ${PORT:0}

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: https://tc2-microservice-discovery-server.onrender.com/eureka/
  instance:
    instance-id: ${spring.application.name}:${eureka.application.instance-id:${random.value}}
    hostname: ${RENDER_EXTERNAL_URL:localhost}
    non-secure-port-enabled: false
    secure-port-enabled: true
    secure-port: 443
    status-page-url: ${RENDER_EXTERNAL_URL:localhost}/actuator/info
    health-check-url: ${RENDER_EXTERNAL_URL:localhost}/actuator/health